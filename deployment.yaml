apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-project
spec:
  replicas: 1
  selector:
    matchLabels:   
      app:web-project-app
  template:
    metadata:
      labels: 
        app:web-project-app
    spec:
      containers:
        - name:web-project-app
          image: firesofttech/complete-prodcution-e2e-pipeline:1.0.0-4
          resources:
            limits:
              memory: "256Mi"
              cpu: "500m"
          ports:
            - containerPort: 8080  
